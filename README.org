#+setupfile: ~/.org-mode-goodies/setup.org

* Cursor Chat History Exporter

This project provides tools to extract and export AI service prompts from Cursor's workspace history databases. It is designed for users who want to analyze or archive their chat history from the Cursor editor.

* Badges
  [[https://opensource.org/licenses/MIT][MIT License]]

* Quickstart

#+BEGIN_SRC shell
git clone <repo-url> Cursor.Chat-History
cd Cursor.Chat-History
make install
make run
#+END_SRC

* Features
- Recursively finds all ~state.vscdb~ files in Cursor workspace storage
- Extracts ~aiService.prompts~ from each database
- Exports prompts to Org-mode files for easy reading and organization
- Supports both multi-workspace and single-file modes
- Modern Python: functional programming, dataclasses, static methods, context managers

* Requirements
- Python 3.8+
- ~uv~ (for dependency management)
- ~duckdb~
- ~pytest~ (for running tests)

* Usage

** Export All Workspace Prompts

#+BEGIN_SRC shell
make run
#+END_SRC

This will:
- Install the package in editable mode
- Run the exporter, which scans all Cursor workspaceStorage directories and exports prompts to ~chat_history_exports/*.org~

** Export Prompts from a Specific File

#+BEGIN_SRC shell
python cursor_chat_history.py /path/to/state.vscdb
#+END_SRC

* Testing

Run the test suite with:

#+BEGIN_SRC shell
python3 run_tests.py
#+END_SRC

Or run individual tests:

#+BEGIN_SRC shell
python3 -c "from tests.test_cursor_chat_history import test_timestamp_formatting; test_timestamp_formatting()"
#+END_SRC

** Test Coverage

The test suite covers:
- Original functionality (prompt extraction)
- New timestamp functionality
- Backward compatibility
- Error handling
- File modification time setting
- Org mode formatting
- Mixed timestamp scenarios

* Project Structure

- ~cursor_chat_history.py~ :: Main script and library
- ~tests/~ :: Unit tests
- ~chat_history_exports/~ :: Output directory for exported Org files

* Example Output

Each exported Org file will contain entries (your past prompts) with timestamps like:

#+BEGIN_EXAMPLE
* [2024-12-24 Tue 10:35] What is the capital of France?
* [2024-12-24 Tue 10:35] Summarize this article.
#+END_EXAMPLE

** Timestamp Information

The timestamps shown in the exported files represent the last modification time of the source database file. This provides a reasonable approximation of when the chat data was last updated, though it may not reflect the exact time of individual prompts.

** Timestamp Format

Timestamps are formatted as ~[YYYY-MM-DD Day HH:MM]~ where:
- ~YYYY-MM-DD~ is the date
- ~Day~ is the three-letter day abbreviation (Mon, Tue, etc.)
- ~HH:MM~ is the time in 24-hour format

* Troubleshooting

- If you see errors about missing ~duckdb~, install it with ~uv pip install duckdb~.
- If no files are exported, check that you have Cursor installed and have used it to generate workspace history.
- If timestamps appear incorrect, they represent the file modification time of the database, not individual prompt times.

* Contributing

Contributions are welcome! Please open issues or submit pull requests.

* License

MIT License (see LICENSE file) 
